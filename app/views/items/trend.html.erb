    <div class="container">
        <section class="content-main">
            <div class="center-block">
                <div class="col-md-10 col-xs-offset-1">
                    <!--search box-->
                    <form action="" method="post">
                    <div class="input-group">
                      <input type="text" class="form-control" name="search_word" placeholder="項目検索">
                      <span class="input-group-btn">
                        <button class="btn btn-pink" type="submit">
                          <i class='glyphicon glyphicon-search'></i>
                        </button>
                      </span>
                    </div>
                    </form>
                </div>
            </div>
        </section>
    </div>

<br>
<div class="container">
    <div class="center-block">
    <div class="col-md-8 col-xs-offset-2">
          <ul class="nav nav-pills nav-justified">
          <% if @genre == 'hot' %>
              <li><a href="#">NEW</a></li>
              <li class="active"><a href="#">HOT</a></li>
          <% else %>
              <li class="active"><a href="#">NEW</a></li>
              <li><a href="#">HOT</a></li>
          <% end %>
          </ul>
    </div>
    </div>
</div>
 <br />
    <div class="container">
      <div class="row">
        <section id="pinBoot">

          <!--content-->
<% @items.each do |item| %>
          <article class="white-panel">
            <a href="" data-toggle="modal" data-target="#item_detail" data-title="<%= item.name %>" data-comment="<%= item.comment %>" data-done="13" data-doing="122" data-id="<%= item.id %>">
                <h4 class="item_name"><%= item.name %></h4>
                <span class="doing">doing 122</span>
                <span class="done">done 13</span>
            </a>
            <p>
            <button type="button" class="btn btn-pink btn-circle center-block" data-toggle="modal" data-target="#add_new" data-id="<%= item.id %>" data-title="<%= item.name %>" data-comment="<%= item.comment %>">
                <i class="fa fa-plus" aria-hidden="true"></i>
            </button>
            </p>
          </article>
<% end %>

        </section>
        </div>
    </div>

    <!-- <?php include('views/items/add_trend.php'); ?> -->
    <%= render 'items/add_trend' %>

    <!-- <?php include('views/items/show_trend.php'); ?> -->
    <%= render 'items/show_trend' %>
