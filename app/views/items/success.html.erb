        <div class="container-fluid">
          <div class="row">

            <section class="col-sm-10 col-xs-offset-1 main-content">

                <!--項目一覧-->
                 <ul class="list-unstyled">
            <% @items.each.with_index(1) do |item, i| %>
                    <a data-toggle="modal" href="" data-target="#item_success" data-name="item_id" data-title="<%= item.name %>" data-id="<%= item.id %>" data-comment="<%= item.comment %>" data-created="<%= item.created_at %>" data-priority="<%= item.priority %>">
                    <li>
                        <%= i %>
                        <dl>
                            <dt class="success"><%= item.name %></dt>
                        <% if true %>
                            <dd><a data-toggle="modal" href="" data-target="#edit_success" data-title="<%= item.name %>" data-comment="<%= item.comment %>" data-id="<%= item.id %>"><i class="fa fa-pencil" aria-hidden="true"></i></a></dd>
                        <% end %>
                            <p><i class="fa fa-calendar" aria-hidden="true"></i>&nbsp;&nbsp;<%= item.created_at %></p>
                        </dl>
                    </li>
                    </a>
            <% end %>
                </ul>
            </section>
          </div>
        </div>


    <!--modal item_success-->
          <!-- <?php include('views/items/show_success.php'); ?> -->
        <%= render 'items/show_success' %>

    <!--modal edit_success-->
          <!-- <?php include('views/items/edit_success.php'); ?> -->
        <%= render 'items/edit_success' %>
